# 기획 보완 목록

기획서(`docs/product-spec.md`)와 실제 구현을 대조해서 나온 항목들. 대부분 "빠진 기능"이 아니라
**기획서가 스스로 약속했는데 코드가 지키지 못하고 있는 지점**이다.

작성: 2026-07-27

## 우선순위

1. **1~9 전부 완료** (2026-07-29 ~ 08-08). 기획서 5.4~5.12절
2. **목록에 남은 작업 없음.** 4번 뒷부분은 5.15절로, 6번 뒷부분은 5.16절로 대체했고
   10번은 마감까지 끝났다 (letter-spacing만 파급 범위 커서 보류)
3. 기다리는 것: **표현 재사용률** — 2026-08-09 기준 62개 중 2개. 2주 뒤 표현 페이지의
   "다시 쓴 것" 숫자를 보고 안 움직이면 그때 복습 세션을 만든다
   - 중간 측정 (2026-08-15): 101개 중 3개. **되살리기 기능 이후 배운 45개 중 재사용 0개**이고,
     3개는 전부 기능 이전에 배운 것이다. 8월 23일까지 그대로 두고 본다 (2026-08-15 결정)
   - 그때 원인은 여전히 모를 것이다 — 힌트를 저장하지 않아서 "지난 표현이 힌트에 안 들어갔다"와
     "들어갔는데 안 썼다"를 구분할 수 없다. 처방이 정반대인 두 경우다

---

## 1. "늘고 있나"를 재는 지표가 작동하지 않는다

기획서 4.4절은 실수 추이가 대시보드의 핵심이고 나머지는 다 "얼마나 했나"라고 못박았다
(`docs/product-spec.md:78`). 그런데 프롬프트가 매번 **"3~5개만"** 을 요구한다
(`src/lib/prompts.ts:86`) — 바닥과 천장이 동시에 박혀 있어서 실력이 늘어도 그래프는 평평하다.
답변 길이로 나누지도 않아서 짧게 쓴 날이 잘한 날처럼 보인다.

- [x] 지표를 **재발률**로 교체 (2026-07-30). 실측으로 확인: 실수 개수는 10세션 내내 3·4·5뿐이라
      애초에 측정이 아니었음
- [~] 밀도는 **채택 안 함** — 같은 데이터에서 30단어 답변이 최악, 103단어가 최고로 나옴.
      길게 쓰면 자동으로 좋아 보이는 지표라 "분량 아닌 꾸준함" 원칙과 충돌
- [x] 기획서 5.4절 다시 씀 (왜 개수를 버렸고 왜 밀도를 안 골랐는지 데이터와 함께)

## 2. 그 지표를 떠받치는 태그가 자유 문자열이다

`tag`가 스키마상 그냥 `string`이고(`src/lib/schemas.ts:33`) 프롬프트는 예시 세 개만 준다.
모델이 `article-omission` / `missing-article` / `articles`를 섞어 쓰면 8절이 약속한
"반복 실수 패턴" 집계가 조용히 무너진다. 실패해도 에러가 안 나서 더 위험하다.

- [x] 태그 사전 27개 확정 (2026-07-29). `src/lib/tags.ts`, 8개 묶음
- [x] 스키마 enum으로 조임 — 이제 모델이 태그를 지어내면 프로바이더가 거절
- [x] 기존 20건 마이그레이션 (`npm run retag`). 16종 → 9종

## 3. "생각이 안 나요"의 두 번째 벽이 기획에 없다

앱의 존재 이유라고 선언한 문제인데(`docs/product-spec.md:21`) 대책이 힌트 하나뿐이다.
힌트를 봐도 안 떠오를 때, 혹은 떠올랐는데 영어로 못 옮길 때의 탈출구가 없다.
그 순간의 행동은 ChatGPT 때와 똑같이 "창 닫기"다. 3~10문장이 사실상 진입 장벽으로 작동한다.

- [x] 한 문장만 써도 통과 (2026-07-30). 기획서 5.6절 "가볍게 한 문장"
- [x] 더 쉬운 질문으로 바꾸기 (2026-08-02). 힌트 카드 아래 "더 쉬운 질문으로",
      질문 하나당 한 번. 5.6절의 가벼운 모드를 그대로 재사용해서 난이도 축을 늘리지 않음
- [x] 한국어로 적으면 첫 문장만 영어로 시작해주기 (2026-08-02). 한 문장만,
      원래 쓴 한국어는 옆에 남겨서 보고 이어 쓰게. 기획서 5.8절

## 4. 배운 것이 입력으로 돌아오지 않는다

한 줄 정의는 "기록이 쌓이는 것을 눈으로 보는" 앱인데, 쌓인 표현은
**"다시 가르치지 마라" 블랙리스트로만** 쓰인다(`src/lib/prompts.ts:53`).
한 번 본 표현은 두 번 다시 안 나온다. `expressions`의 유니크 제약도 이 방향을 강화한다.
로드맵 2단계의 "표현 목록 페이지"는 수동으로 들여다보는 화면이라 습관 앱에서 가장 안 쓰인다.

- [x] 다음 질문이 지난 표현을 쓰도록 유도 (2026-08-01). 표현 목록이 블랙리스트에서
      "쓸 자리를 만들어라"로 바뀜. 오래된 것 우선(최근 것은 아직 안 잊음).
      학습자가 실제로 쓰면 칭찬에서 짚어줌
- [x] 배운 표현을 **힌트로** 되돌리기 (2026-08-09). 기획서 5.15절.
      실측으로 5.4b가 안 먹힌 걸 확인 — 62개 배워서 다시 쓴 건 2개(3%).
      힌트와 표현이 같은 모양(61/62가 2~6단어)이라 힌트 한 칸을 지난 표현에 준다
- [~] 주 1회 복습 세션은 **보류** — todo 4번이 스스로 적어둔 "수동 화면은 안 쓰인다"에
      걸리고, 주 1회 5개는 주당 20개 쌓이는 속도에 못 미친다. 2주 뒤 재사용률
      (표현 페이지의 "다시 쓴 것") 보고 안 움직이면 그때 만든다. 기준선 2/62

## 5. 하루 한도와 "대화는 끝나지 않는다"가 정면충돌한다

11절은 무료 한도를 기술 리스크로만 적어뒀지만, 실제로는 **불변 규칙 하나를 매일 깨뜨린다.**
4번째 답변쯤 429가 나면 "끝나지 않는 대화"가 에러 토스트로 끝난다.

- [x] 한도 소진을 예외 처리가 아니라 **기획된 순간**으로 만들기 (2026-08-04). 기획서 5.9절.
      진짜 손실은 피드백이 아니라 **답변 자체**였다 — 채점에 실패하면 쓴 글이 사라지고
      그날이 연속 학습일에서 빠졌다. 이제 어떤 실패든 답변은 저장되고, 홈 맨 위
      "이어서 하기"에서 나중에 피드백을 받는다
- [~] "내일의 첫 질문 미리 저장"은 채택 안 함 — 한도가 떨어진 시점엔 질문을 만들 호출이
      없고, 손에 있는 후속 질문은 방금 답한 그 질문이다. 저장된 답변이 더 나은 장치다

## 6. 소재 고갈이 이탈 1순위인데 3단계로 밀려 있다

토픽 9개면 매일 해서 2주에 한 바퀴. 고향·연애는 세 번째부터 할 말이 없다.
더 큰 문제는 개인화 재료가 없다는 것 — DB 프로필에 `level`과 `english_variant`뿐이라
(`docs/product-spec.md:124-129`) 같은 토픽에서 질문이 갈라질 근거 자체가 없다.

- [x] 프로필에 "나에 대해" 필드 추가 (2026-08-02). 홈 기록 섹션에서 편집, 400자 제한.
      실측: 같은 취미 토픽이 "그만둔 취미가 뭐예요" → "스쿠버 자격증은 어떻게 돼가요"로 바뀜
- [~] 토픽 직접 추가는 **보류** (2026-08-10 판단). 실측상 9개도 다 안 쓰고 있다 —
      2~3개에 몰려 있고 하나는 한 번도 안 열었다. "9개로 모자라서"가 아니라
      "안 끌려서" 안 쓰는 것이므로 토픽을 늘려도 같은 결과다
- [x] 대신 **가벼운 날의 토픽 선택을 밀린 순서로** (2026-08-11). 기획서 5.16절.
      균등 랜덤이라 어제 한 걸 또 줄 확률이 2/9였다. 이제 가장 오래된 3개 중에서 뽑는다 —
      결정을 하나도 더 요구하지 않으면서 재료가 넓어지는 유일한 자리

## 7. 레벨 판정 규칙이 기획서에 없다

매 답변마다 모델이 CEFR을 다시 추정한다(`src/lib/prompts.ts:94`, 가드는 "크게 흔들리지 말라"
한 줄). 이 값이 질문 난이도와 🚀 트로피를 동시에 좌우한다(`src/lib/stats.ts:104-106`).
피곤해서 짧게 쓴 날 B1로 내려갔다가 다음날 B2 트로피가 다시 뜨면 트로피 전체가 헐값이 된다.

- [x] 승급 조건 명시 (2026-08-06). **최근 5회 중 3회가 다음 레벨 이상이면 한 칸.
      절대 안 내려감.** 기획서 5.10절
- [x] 진단이 반쯤 틀렸던 것도 기록: 걱정한 건 출렁임인데 실제로는 13회 연속 B1로
      **얼어붙어 있었다.** 원인은 프로필 브리프가 현재 레벨을 같이 넘긴 앵커링 —
      빼니까 같은 답변이 B1 → A2로 갈렸다

## 8. streak이 유일한 동기 장치인데 끊김 설계가 없다

6절에서 **"일주일 쉬었다고 30일 기록이 0이 되면 다시 시작할 이유가 사라진다"** 고 정확히
진단해놓고(`docs/product-spec.md:95`) 해결한 건 데이터 유실 경로뿐이다. 여행 가거나 아파서
이틀 빠져도 결과는 똑같이 0이다. 진단은 맞았는데 처방이 절반만 됐다.

- [x] 하루 유예 + 30일당 2회 (2026-08-01). 기획서 5.7절.
      주 단위 목표와 프리즈 카드는 채택 안 함 (이유는 5.7절에)

## 9. 모델이 틀렸을 때의 경로가 없다

잘못된 교정이 `mistakes` 테이블에 그대로 남아 잘못 배우는 것 + 통계 오염이 동시에 일어난다.
혼자 쓰는 앱이라 신고 기능은 과하다.

- [x] 실수 항목 하나를 "이 교정은 빼주세요"로 지우기 (2026-08-08). 기획서 5.12절.
      행은 `dismissed_at`으로 남기고 세는 쿼리에서만 뺀다 — 모델이 틀렸다는 기록이라서.
      재발률의 "처음 본 시점"에서도 빠짐

---

## 10. 애플 디자인 스킬 적용 후보 (모션 다듬기)

`apple-design` 스킬로 코드 리뷰한 결과. 위 항목들과 달리 기획 위반이 아니라 마감 다듬기라
우선순위 밖. 폰 미지원 스코프(`docs/product-spec.md:315`)라 드래그·스와이프·러버밴딩 계열
원칙은 애초에 적용 대상이 없어서 제외.

- [x] 뱃지 토스트에 exit 애니메이션 — `animate-rise-out` 추가, `BadgeStack`으로 분리해
      배치별로 remount하며 5초 뒤 페이드아웃 (`src/app/page.tsx`, `src/app/globals.css`)
- [x] 힌트 카드 접기 애니메이션 — `grid-template-rows` 트랜지션으로 펼침/접힘을
      대칭으로 처리 (`src/components/Session.tsx`)
- [x] hover에 스프링 커브 (2026-08-10). 항목 이름이 오해였다 — **색과 그림자는
      오버슈트가 의미 없어서** 곡선만 바꿀 데가 없었다. 움직임을 먼저 넣어야 했다.
      카드형 두 곳(`Home.tsx` 토픽 카드, `History.tsx` 세션 행)에 2px 부상 +
      `--ease-spring`. 버튼은 색만 바뀌므로 그대로 둠
      - 함정: Tailwind v4는 `-translate-y-*`를 `transform`이 아니라 **`translate`
        프로퍼티**로 컴파일한다. `transition-[transform,…]`으로 쓰면 조용히 안 움직임
- [ ] (선택, 파급 범위 큼) 헤딩 letter-spacing을 크기별로 스케일 — 지금은 전 크기 고정값
      (`src/app/globals.css:54`)
- 보류: backdrop-blur 확장 금지 — 현재 `page.tsx:76` 한 곳만 절제되게 쓰는 중이고,
  늘리면 AGENTS.md가 명시한 "두 번 거부당한 글래스모피즘"과 충돌
