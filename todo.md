# 기획 보완 목록

기획서(`docs/product-spec.md`)와 실제 구현을 대조해서 나온 항목들. 대부분 "빠진 기능"이 아니라
**기획서가 스스로 약속했는데 코드가 지키지 못하고 있는 지점**이다.

작성: 2026-07-27

## 우선순위

1. **1 + 2** — 데이터가 계속 쌓이는 중이라 늦을수록 과거 기록이 못 쓰게 된다
2. **3** — 이 앱의 존재 이유 그 자체
3. **6** — 매일 쓰면 2주 뒤 실제로 부딪히는 벽

4·5는 그 다음, 7·8·9는 문장 몇 줄 수준.

---

## 1. "늘고 있나"를 재는 지표가 작동하지 않는다

기획서 4.4절은 실수 추이가 대시보드의 핵심이고 나머지는 다 "얼마나 했나"라고 못박았다
(`docs/product-spec.md:78`). 그런데 프롬프트가 매번 **"3~5개만"** 을 요구한다
(`src/lib/prompts.ts:86`) — 바닥과 천장이 동시에 박혀 있어서 실력이 늘어도 그래프는 평평하다.
답변 길이로 나누지도 않아서 짧게 쓴 날이 잘한 날처럼 보인다.

- [ ] 지표를 **재발률**(이전에 지적받은 태그가 다시 나왔나)로 교체 — 가장 정직함
- [ ] 차선: 100단어당 실수 밀도
- [ ] 기획서 4.4절 다시 쓰기

## 2. 그 지표를 떠받치는 태그가 자유 문자열이다

`tag`가 스키마상 그냥 `string`이고(`src/lib/schemas.ts:33`) 프롬프트는 예시 세 개만 준다.
모델이 `article-omission` / `missing-article` / `articles`를 섞어 쓰면 8절이 약속한
"반복 실수 패턴" 집계가 조용히 무너진다. 실패해도 에러가 안 나서 더 위험하다.

- [ ] 태그 사전(20~30개 고정 enum) 확정 — **코드가 아니라 기획서에 있어야 할 항목**
- [ ] 스키마를 enum으로 조이고 프롬프트에 목록 전달
- [ ] 이미 쌓인 태그 정규화(마이그레이션)

## 3. "생각이 안 나요"의 두 번째 벽이 기획에 없다

앱의 존재 이유라고 선언한 문제인데(`docs/product-spec.md:21`) 대책이 힌트 하나뿐이다.
힌트를 봐도 안 떠오를 때, 혹은 떠올랐는데 영어로 못 옮길 때의 탈출구가 없다.
그 순간의 행동은 ChatGPT 때와 똑같이 "창 닫기"다. 3~10문장이 사실상 진입 장벽으로 작동한다.

- [ ] 더 쉬운 질문으로 바꾸기
- [ ] 한국어로 적으면 첫 문장만 영어로 시작해주기
- [ ] 한 문장만 써도 통과 (짧은 답변도 정식 세션으로)

## 4. 배운 것이 입력으로 돌아오지 않는다

한 줄 정의는 "기록이 쌓이는 것을 눈으로 보는" 앱인데, 쌓인 표현은
**"다시 가르치지 마라" 블랙리스트로만** 쓰인다(`src/lib/prompts.ts:53`).
한 번 본 표현은 두 번 다시 안 나온다. `expressions`의 유니크 제약도 이 방향을 강화한다.
로드맵 2단계의 "표현 목록 페이지"는 수동으로 들여다보는 화면이라 습관 앱에서 가장 안 쓰인다.

- [ ] 다음 질문이 지난 표현을 자연스럽게 쓰도록 유도 (가장 저렴하고 확실)
- [ ] 주 1회 복습 세션은 그 다음

## 5. 하루 한도와 "대화는 끝나지 않는다"가 정면충돌한다

11절은 무료 한도를 기술 리스크로만 적어뒀지만, 실제로는 **불변 규칙 하나를 매일 깨뜨린다.**
4번째 답변쯤 429가 나면 "끝나지 않는 대화"가 에러 토스트로 끝난다.

- [ ] 한도 소진을 예외 처리가 아니라 **기획된 순간**으로 만들기 —
      오늘의 요약 + 내일의 첫 질문을 미리 저장해두면 강제 종료가 "내일 이어서" 장치가 된다

## 6. 소재 고갈이 이탈 1순위인데 3단계로 밀려 있다

토픽 9개면 매일 해서 2주에 한 바퀴. 고향·연애는 세 번째부터 할 말이 없다.
더 큰 문제는 개인화 재료가 없다는 것 — DB 프로필에 `level`과 `english_variant`뿐이라
(`docs/product-spec.md:124-129`) 같은 토픽에서 질문이 갈라질 근거 자체가 없다.

- [ ] 프로필에 "요즘 하는 일 / 관심사" 두어 줄 필드 추가 → 9개 토픽이 훨씬 오래 버틴다
- [ ] 토픽 직접 추가는 그 다음

## 7. 레벨 판정 규칙이 기획서에 없다

매 답변마다 모델이 CEFR을 다시 추정한다(`src/lib/prompts.ts:94`, 가드는 "크게 흔들리지 말라"
한 줄). 이 값이 질문 난이도와 🚀 트로피를 동시에 좌우한다(`src/lib/stats.ts:104-106`).
피곤해서 짧게 쓴 날 B1로 내려갔다가 다음날 B2 트로피가 다시 뜨면 트로피 전체가 헐값이 된다.

- [ ] 승급 조건 명시 (예: 최근 N세션 중 M개 이상) — 기획 결정 사항

## 8. streak이 유일한 동기 장치인데 끊김 설계가 없다

6절에서 **"일주일 쉬었다고 30일 기록이 0이 되면 다시 시작할 이유가 사라진다"** 고 정확히
진단해놓고(`docs/product-spec.md:95`) 해결한 건 데이터 유실 경로뿐이다. 여행 가거나 아파서
이틀 빠져도 결과는 똑같이 0이다. 진단은 맞았는데 처방이 절반만 됐다.

- [ ] 주 단위 목표 또는 freeze 하루

## 9. 모델이 틀렸을 때의 경로가 없다

잘못된 교정이 `mistakes` 테이블에 그대로 남아 잘못 배우는 것 + 통계 오염이 동시에 일어난다.
혼자 쓰는 앱이라 신고 기능은 과하다.

- [ ] 실수 항목 하나를 "이건 아닌 것 같아요"로 지우기
